/**
 * The kitchen-sink demo: one runnable program that exercises every widget and subsystem,
 * with the screenshot and benchmark harnesses the project's visual verification runs
 * through. This module consumes the toolkit rather than belonging to it: it is not
 * published as a library, nothing may depend on it, and nothing in it is API.
 */
package limn.demo;
