/**
 * A gallery of components built for a screen reader rather than for a screenshot: one small
 * scene per accessible component, each labelled the way the toolkit intends, which is what the
 * gallery-wide invariant test in this module's tests runs over and what a reader on a guest is
 * pointed at.
 *
 * <p>The same package name exists in this module's test sources, holding the headless window and
 * backend the transcript tests bind a scene to. That is on purpose: the test that reads this
 * gallery sits beside them.
 */
package limn.demo.a11y;
