/**
 * The Tabler icon pack as compile-time constants: every constant of the {@code Tabler*}
 * enums is a {@link limn.icons.tabler.TablerIcon} whose {@code icon()} answers a shared,
 * lazily rasterized {@link limn.graphics.Icon}, and {@link limn.icons.tabler.Tabler} looks
 * the same drawings up by name for when the name is data. An icon pack is an application's
 * choice: nothing in the toolkit depends on this module, and an application that ships its
 * own icons never carries it.
 */
package limn.icons.tabler;
